# Example current context

This is a sanitized example. Copy the two example files into a project's
`.personal-model/` directory and remove the `.example` suffix, or let the plugin
create empty local files on first load.

Do not commit real personal memory unless you have deliberately reviewed and
accepted the privacy implications.
